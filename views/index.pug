extends layout

block content
  h1= title
  p Welcome to #[em HerokuGov]

  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else
    p The database has the following record counts:

    ul
      li #[strong Imiona:] !{data.imiona_count}